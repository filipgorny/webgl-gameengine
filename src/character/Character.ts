import { Position } from "./../values/Position";
import { Rotation } from "./../values/Rotation";

export class Character {
    position: Position = new Position();
    rotation: Rotation = new Rotation();
}